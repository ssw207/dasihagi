# 메타 프롬프트: 프리셋 여러 개 한 번에 삭제

## 역할

너는 Chrome 확장 프로그램(폼 프리셋, MV3)의 팝업 UX를 고치는 시니어 프론트엔드 엔지니어다. 아래를 **코드 탐색 후** 최소 변경으로 구현하고 검증까지 완료한다.

## 배경 / 사용자 요구 원문

> 프리셋 한번에 여러개 삭제 할수 있는 기능 추가

지금은 카드마다 **삭제**를 눌러 하나씩만 지울 수 있다 (`PRESET_DELETE` + confirm). 프리셋이 많으면 반복이 크다.

## 목표 동작

1. 목록에서 프리셋을 **여러 개 선택**할 수 있다.
2. **선택 삭제**로 한 번에 지운다. 확인 창에 개수를 보여 준다.
3. **전체 선택 / 전체 해제**가 있다.
4. 기존 카드 **삭제**(1개)와 편집 화면 삭제는 그대로다.
5. 지운 프리셋을 쓰던 그룹 스텝은 지금과 같이 빠진다 (blob + 인덱스도 정리).

## 핵심 설계

- 메시지 `PRESET_DELETE_MANY { ids: string[] }`. 내부에서 기존 `deletePreset`를 돌리고, 그룹 스텝은 **한 번만** 걸러 저장한다.
- `PRESET_DELETE`는 같은 헬퍼를 `[id]`로 호출해 동작이 갈라지지 않게 한다.
- 상한: `ids`는 문자열만, 중복 제거, 최대 `MAX_IMPORT_PRESETS`(500). 빈 배열은 오류.
- 팝업: 카드 왼쪽에 체크박스, 툴바에 `전체 선택` + `선택 삭제`. 0개면 삭제 버튼 비활성.
- 확인: `선택한 프리셋 N개를 삭제할까요?` (그룹 스텝이 빠진다는 한 줄 안내 가능).
- UI 문자열 한국어. 체크 클릭이 재생/편집을 트리거하면 안 된다.

## 변경 범위 (파일별)

| 파일 | 변경 |
|------|------|
| `background.js` | `deletePresets(ids)`, `PRESET_DELETE`/`PRESET_DELETE_MANY` |
| `popup/popup.html` | 전체 선택 + 선택 삭제 툴바 |
| `popup/popup.js` | 선택 Set, 체크박스, 일괄 삭제 |
| `popup/popup.css` | 선택 체크·툴바 |
| `test/encryption-verify.mjs` | 여러 개 삭제 + 그룹 스텝 정리 |
| `REQUIREMENTS.md` / `decisions.md` / `README.md` | R14, D27 |

`secure-store.js` / `content.js` 변경 금지.

## 금지 사항 (MUST NOT)

- 확인 없이 삭제.
- 선택되지 않은 프리셋 삭제.
- 그룹 자체 삭제 (스텝에서 해당 presetId만 제거).
- 기존 1개 삭제 UX 제거.
- `as any` / `@ts-ignore` / 빈 catch.
- 테스트 삭제로 통과시키기.

## 검증

- [ ] 2개 이상 선택 → 확인 → 모두 삭제, blob/인덱스 없음
- [ ] 0개 선택 시 삭제 버튼 비활성
- [ ] 전체 선택 후 삭제
- [ ] 그룹 스텝에서 삭제된 프리셋만 빠짐
- [ ] 1개 삭제는 그대로
- [ ] `npm run check` / `test:encrypt` / `test:pii` / `test:replay` / `npm run build`

## 참고

- 기존 형식: `메타-프롬프트-녹화-필드명-수정.md`
- 관련: R1-1, R6-7 (`deletePreset`)
- 프로젝트 루트: `C:\Users\hoo kim\orca\projects\my-chrome-extension`
