# 메타프롬프트 — 녹화 중 A→B 팝업 창 이어짐

## 목표

사용자 요구 원문: **「녹화 시작시 A사이트에서 B사이트로 팝업이 뜬 경우는 녹화가 안되는거 같은데」**

A 탭에서 녹화 중 `window.open`/대상 `_blank`로 B 팝업(새 탭·새 창)이 열리면, 그 창에서도 녹화가 이어지게 한다. 팝업이 닫히면 A 탭 녹화를 유지한다.

## 변경 범위

| 파일 | 내용 |
|------|------|
| `background.js` | 녹화 탭이 opener인 새 탭/창에 세션 복제·재개, 사이트 허용 목록 병합, 팝업 닫힘 시 A 세션 유지 |
| `content.js` | 필요 시 팝업 탭에서 칩/리스너 복원 (기존 RECORD_START resume 재사용) |
| `test/record-replay-verify.mjs` | opener 탭 녹화 중 새 탭 생성 → B에서 이벤트 기록 |
| `REQUIREMENTS.md` / `decisions.md` / `README.md` | R16, D30 |

## 구현 상세

1. `chrome.tabs.onCreated` / `onUpdated`에서 `tab.openerTabId`가 녹화 세션을 가진 탭이면:
   - 같은 `presetId`로 팝업 탭 세션을 만든다 (이벤트는 공유 또는 종료 시 병합).
   - complete 후 `RECORD_START { resume }`로 칩을 띄운다.
   - B URL을 `allowedSites`에 넣는다.
2. 이벤트는 **프리셋 하나**에 시간 순으로 남긴다. 팝업 세션 이벤트를 A 세션에 이어 붙이거나, 탭별 세션을 두고 `RECORD_STOP` 때 모두 합친다.
3. 팝업 탭이 닫혀도 A 녹화는 계속된다.
4. `chrome://` 등 제한 URL 팝업은 무시.
5. 사이트 하드코딩 금지.

## MUST NOT

- 그룹 실행 탭을 녹화 세션으로 오인하지 않는다.
- 기존 같은 탭 이동 녹화(R11)를 깨지 않는다.
- 테스트 삭제 금지.

## 검증 방법

- `npm run check` / `test:encrypt` / `test:pii` / `test:replay`
- replay에 opener→새 탭 시나리오 추가
- 가능하면 `npm run test:e2e`
