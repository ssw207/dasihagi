# 메타프롬프트 — README를 깃허브 공개 저장소 기준으로 전면 갱신

## 목표

사용자 요구 원문: **「README도 최신으로 업데이트, 어떻게 설치하고 사용하는지 README 만 보연 알수있게 해야한다. 지금은 그렇지 못함. 소스가 퍼블릭 깃허브에 저장되어있다고 가정」**

개발자가 아닌 한국어 사용자가 README만 읽고 클론 → 빌드 → 크롬 로드 → 녹화/재생/그룹까지 할 수 있게 한다.

## 변경 범위

- `README.md`만 (필요하면 REQUIREMENTS 변경 이력 한 줄)
- 코드 로직 변경 없음

## 구현 상세

1. 가정: 공개 GitHub. 구체 소유자/레포 이름은 모르면 `<저장소 URL>`로 두고 클론 절차를 적는다.
2. 필수: Node.js, Chrome, git.
3. Windows 기준으로 쓰되 mac/Linux 한 줄씩.
4. 최신 기능: 이 사이트 녹화, 현재 사이트만, ⋯ 메뉴, 여정, 팝업 창, iframe, 재생 속도, 여러 개 삭제, 암호화, 그룹 vs 여정.
5. 제한(캡차, 파일첨부, sandbox iframe, 동적 좌석)을 짧게.
6. 테스트 페이지 안내는 선택. 긴 heredoc 테스트 HTML은 빼고 저장소의 `test/test-form.html`을 쓰게 한다.
7. 움짤이 있으면 유지.

## MUST NOT

- 가짜 깃허브 주소 날조 금지 (`https://github.com/you/repo` 같은 가짜 소유자명 금지). `git clone <이 저장소 URL>` 형식.
- 내부 결정 ID(D32 등)를 사용 설명에 나열하지 않음.
- 구현 코드 변경 금지.

## 검증

- README만 읽고 설치 단계가 빠짐없이 이어짐
- 현재 제품 UI 문구와 버튼 이름이 일치
