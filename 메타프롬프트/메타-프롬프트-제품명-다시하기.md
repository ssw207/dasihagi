# 메타프롬프트 — 제품명 다시하기 (dasihagi)

## 목표

사용자 요구 원문: **「추천대로 진행. 이름이 바뀜에 따라 소스에서 프로젝트명이 있는부분도 같이 수정해」**

표시 이름: **다시하기**. 저장소/패키지: **dasihagi**. UI·매니페스트·package·README·내보내기 appId를 맞춘다. 가져오기는 예전 `form-preset-extension`도 받는다.

## 변경 범위

- `manifest.json`, `package.json`, `popup/*`, `content.js`(칩/패널에 제품명이 있으면), `README.md`
- 내보내기 `appId`, 가져오기 호환
- 테스트에 박힌 앱 이름/문자열
- GitHub `ssw207/dasihagi` 생성·푸시는 로그인되어 있으면 진행

## MUST NOT

- 워크스페이스 폴더 경로 강제 이동 금지
- 예전 JSON 가져오기 깨지 않음
- 사이트 하드코딩 금지

## 검증

- `npm run check` / `test:encrypt` / `test:pii` / `test:replay`
- `npm run build`
- UI/매니페스트에 "폼 프리셋"이 제품명으로 남지 않음
