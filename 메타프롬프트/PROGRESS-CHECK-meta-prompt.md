# 진행상황 확인 메타 프롬프트 (폼 프리셋 Chrome Extension)

> **사용법**: `[메타 프롬프트 본문]`을 통째로 복사해 AI(에이전트)에 붙여넣으세요. AI는 프로젝트의 **git 히스토리, 플랜 문서, 요구사항 정의서(REQUIREMENTS.md), 실제 코드를 서로 대조**해 현재 진행 상황을 체크리스트로 보고합니다.
>
> **중점**: 플랜 문서에 적힌 체크박스 상태를 그대로 믿지 않는다. **실제 코드와 머지된 브랜치로 검증된 상태**만 보고한다.
>
> **전제 조건**: 프로젝트 루트(`my-chrome-extension/`)에서 실행. git 저장소여야 함.

---

## [메타 프롬프트 본문]

당신은 프로젝트 진행 상황 감사(audit) 전문가입니다. 아래 Chrome 확장 프로그램(MV3) 프로젝트의 **현재 진행 상황**을 조사하고, 플랜 문서의 체크 표시와 **실제 구현이 일치하는지 검증**한 뒤 체크리스트로 보고하세요.

## 1. 정보 수집 (반드시 아래 순서로, 근거를 수집할 때마다 출처를 기록)

### 1-1. Git 상태
- `git log --oneline -15` — 최근 커밋/머지 히스토리 (커밋 메시지에서 구현된 기능명 식별)
- `git status --short` — 미커밋 변경, 미추적 파일
- `git branch -a` — 브랜치 목록
- 머지 여부 판별: `git branch --merged master` 와 `git branch --no-merged master` (기본 브랜치가 `master`가 아니면 그 브랜치 기준으로 판별)
- 참고: `git log --oneline --graph -10` 으로 머지 구조 확인

### 1-2. 플랜 문서 (`.sisyphus/plans/*.md`)
- 모든 플랜 파일을 읽고 `- [ ]` / `- [x]` 체크박스 상태를 **파일별로 집계**
- 플랜 제목 = 기능 단위로 취급 (예: `form-preset-extension.md` 기본 기능, `group-feature.md` 그룹 실행, `preset-export-import-plan.md` 내보내기/가져오기)

### 1-3. 노트 (`.sisyphus/notepads/**`)
- `decisions.md` — 확정된 아키텍처 결정(D1~)이 실제 코드에 반영됐는지 확인
- `issues.md` / `problems.md` — 미해결 이슈가 있으면 반드시 보고에 포함
- `learnings.md` — 참고 사항

### 1-4. 실제 코드 대조 (구현 검증의 핵심)
- `manifest.json`: MV3 선언, permissions(`storage`/`tabs`/`scripting`), content_scripts, action popup, background service_worker 확인
- `background.js`: 프리셋 CRUD, 그룹 실행 상태 머신, 메시지 라우터 등 핵심 함수 존재 여부
- `content.js`: 캡처 모드(하이라이트/부동 패널/셀렉터 생성)와 적용 모드(타입별 값 세팅) 존재 여부
- `popup/`: 프리셋/그룹/내보내기 UI 존재 여부
- `package.json`: 스크립트(`build`, 테스트) 존재 여부
- `dist/`: 최신 빌드 산출물 존재 여부
- `test/`: 테스트 파일 존재 여부

### 1-5. 요구사항 정의서 (`REQUIREMENTS.md`)
- R1~R8 기능 요구사항(§2), 비기능 요구사항(§3), 검증 체크리스트(§6), 보류/미구현(§8)을 읽는다
- 구현된 각 기능을 요구사항 ID(R1-1 등)에 매핑해 상태를 판정한다
- 이 문서에 없는 코드/기능이 발견되면 "문서화 안 된 작업"으로 표시하고 **REQUIREMENTS.md 최신화가 필요함**을 보고에 포함한다

## 2. 검증 규칙 (반드시 준수)

1. 플랜의 `[x]`(완료) 항목은 **실제 코드에서 근거를 찾은 경우에만** 완료로 인정한다. 코드에서 근거를 찾지 못하면 `⚠️ 불일치`로 표시하고 근거를 명시한다.
2. 기능 브랜치의 머지 여부는 `git branch --merged` 로 판별한다. 머지되지 않은 브랜치는 `📋 대기`로 표시한다.
3. 어느 플랜이나 요구사항 정의서(REQUIREMENTS.md)에도 없는 코드/기능이 발견되면 "문서화 안 된 작업"으로 별도 표시하고, REQUIREMENTS.md 최신화 필요 여부를 보고에 포함한다.
4. 미해결 이슈(`issues.md`, `problems.md`)는 누락 없이 보고에 포함한다.
5. 상태는 추측하지 않는다. 근거를 못 찾으면 "확인 불가"로 표시한다.

## 3. 최종 보고 형식 (반드시 이 형식)

```
# 진행 상황 보고 (YYYY-MM-DD)

## 1. 요약 (3줄 이내)
- 현재 단계: [예: 핵심 기능 + 그룹/내보내기 머지 완료, UI 개선 대기 중]
- 완료율: 플랜 TODO 기준 N% (완료 체크 수 / 전체 체크 수)

## 2. 기능별 상태
| 기능 | 상태 | 근거 |
|------|------|------|
| 기본 프리셋 (T1~T5) | ✅ 완료 | 플랜 [x] + background.js CRUD 코드 + 커밋 <해시> |
| 그룹 실행 | ✅ 완료 | 머지 <해시> + background.js 실행 상태 머신 코드 |
| 내보내기/가져오기 | ✅ 완료 | 머지 <해시> + EXPORT/IMPORT 메시지 코드 |
| [플랜에 있으나 미구현] | 📋 대기 | 플랜 [ ] 상태 + 관련 브랜치 미머지 |
| [플랜과 코드 불일치] | ⚠️ 불일치 | [플랜 표시] ↔ [실제 코드 상태] |

상태 표기: ✅ 완료 / ⏳ 진행 중 / 📋 대기(미착수) / ⚠️ 불일치

> 기능 매핑 기준: REQUIREMENTS.md의 요구사항 ID(R1~R8). 미구현 항목은 REQUIREMENTS.md §8(보류/미구현)과 대조해 표시.

## 3. 미완료·대기 항목
- [ ] 항목: 출처 플랜 + 남은 이유
- [ ] 미머지 브랜치: `<브랜치명>` — 계획된 작업

## 4. 이슈 & 리스크
- issues/problems.md 내용 또는 git 상태에서 발견된 것 (각 1줄 + 출처)

## 5. 다음 권장 액션 (1~2개)
- 가장 우선순위가 높은 것부터, 이유와 함께
```

---

## 기대 산출물 요약

```
git 히스토리/브랜치 → 플랜 체크박스 → 노트(결정/이슈) → 실제 코드 대조
→ 기능별 상태 테이블 (검증된 근거 포함)
→ 미완료·대기 항목 → 이슈·리스크 → 다음 권장 액션
```

플랜 문서의 표시가 아니라 **코드와 git으로 검증된 현재 위치**가 산출물입니다. 미완료 작업을 발견하면 그대로 두고 보고만 하며, 구현을 시작하지 않습니다.