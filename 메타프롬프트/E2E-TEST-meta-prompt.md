# E2E 테스트 메타 프롬프트 (폼 프리셋 Chrome Extension — 그룹 실행 기능)

> **사용법**: `[메타 프롬프트 본문]`을 통째로 복사해 AI(에이전트)에 붙여넣으세요. AI는 **Playwright를 직접 구동**해 확장 프로그램을 로드하고, 아래 시나리오를 순서대로 실행하며 검증합니다.
>
> **전제 조건**:
> - `npm run build` 실행 → `dist/` 폴더가 최신 상태
> - 테스트용 로컬 페이지 2개 필요 (`test/form-a.html`, `test/form-b.html`) — 없으면 1번 시나리오 전에 생성
> - AI가 Playwright MCP 서버에 접근 가능해야 함 (`playwright` skill 로드 필수)

---

## [메타 프롬프트 본문]

당신은 Chrome 확장 프로그램 E2E 테스트 전문가입니다. **Playwright 브라우저 자동화**를 직접 구동하여 아래 확장 프로그램의 **그룹(여러 페이지 복합 세팅) 실행 기능**을 엔드투엔드로 검증하세요. 결과는 체크리스트로 보고합니다.

## 0. 작업 전 준비

1. `playwright` skill을 로드합니다. (없으면 "playwright skill을 사용할 수 없다"고 보고하고 중단)
2. 프로젝트 루트에서 `npm run build`가 이미 실행되었는지 확인합니다. `dist/` 폴더가 없거나 최신이 아니면 빌드합니다.
3. `test/form-a.html`, `test/form-b.html`이 없으면 아래 기준으로 생성합니다 (실제 제휴사 페이지를 흉내 낸 서로 다른 폼):
   - `form-a.html`: `<input name="company">`, `<input name="email">`, `<select name="plan">`, `<button type="submit">` 포함
   - `form-b.html`: `<input name="name">`, `<input name="phone">`, `<textarea name="memo">`, `<button type="submit">` 포함
4. 테스트 페이지를 로컬 서버로 띄웁니다: `python3 -m http.server 8765` (또는 `npx serve -l 8765`)
   - **로컬 HTTP 서버를 반드시 사용**합니다. `file://` URL은 확장 content script가 기본적으로 접근 불가하므로 사용하지 않습니다.

## 1. 확장 프로그램 로드 (Playwright)

Playwright로 **확장 프로그램이 로드된 Chrome**을 띄웁니다. MV3 확장은 headless에서 동작하지 않으므로 반드시 다음 방식으로 실행하세요:

```js
const { chromium } = require('playwright');
const path = require('path');

const extPath = path.resolve('<프로젝트 루트 절대 경로>/dist');
const context = await chromium.launchPersistentContext('', {
  headless: false,
  args: [
    `--disable-extensions-except=${extPath}`,
    `--load-extension=${extPath}`
  ]
});
```

- `browser_run_code_unsafe`(또는 이에 준하는 Playwright 실행 도구)로 위 코드를 실행합니다.
- 확장이 로드됐는지 확인: 새 탭에서 `chrome://extensions` 열고 "폼 프리셋"이 존재하는지, 오류("이 패키지가 잘못되었습니다" 등)가 없는지 확인합니다. 로드 실패 시 즉시 보고하고 중단합니다.
- 확장의 service worker가 살아있는지 확인합니다: `chrome://serviceworker-internals`에서 `background.js` 스크립트가 등록되어 있는지 확인합니다.

## 2. 테스트 데이터 초기화

- 확장 저장소가 깨끗한 상태인지 확인합니다. (신규 프로필이면 자동으로 깨끗함)
- 기존 데이터가 있으면 popup에서 프리셋/그룹을 모두 삭제하고 시작합니다.

## 3. E2E 시나리오 (순서대로, 각 시나리오는 독립적으로 검증)

### S1: 프리셋 A 생성 + 캡처 + 적용
1. `http://localhost:8765/test/form-a.html`을 엽니다.
2. 확장 아이콘(툴바의 "폼 프리셋")을 클릭해 popup을 엽니다. (클릭이 안 되면 `chrome://extensions`의 "폼 프리셋"에서 popup 경로를 직접 여는 것으로 대체)
3. **+ 새 프리셋** → 이름 "A 페이지 세팅" → 사이트 패턴이 `localhost:8765`로 자동 입력됐는지 확인 → **저장**
4. **캡처 모드 시작** 클릭 → popup이 닫히고 페이지의 입력란이 파란 점선으로 표시되는지 확인
5. `company` 입력란 클릭 → 표시 이름 "회사명", 값 "테스트컴퍼니" → 저장
6. `email` 입력란 클릭 → "이메일", `tester@example.com` → 저장
7. `plan` select 클릭 → "플랜", 값 "프리미엄" → 저장
8. popup을 다시 열어 **적용** 클릭 → 3개 필드가 모두 채워졌는지 페이지에서 확인 (input value, select 선택값)
9. **통과 조건**: 3개 필드가 저장한 값으로 채워짐, 실패 0개

### S2: 프리셋 B 생성 + 캡처 + 적용
1. S1과 동일한 절차를 `form-b.html`에서 수행 (이름 "B 페이지 세팅", 필드 3개: name/phone/memo)
2. **통과 조건**: 3개 필드가 저장한 값으로 채워짐

### S3: 그룹 생성 (A → B, 수동 제출)
1. popup 상단 **그룹** 탭 클릭 → **+ 새 그룹**
2. 그룹 이름 "제휴사 세팅 테스트"
3. 프리셋 드롭다운에서 "A 페이지 세팅" 선택 → **+ 스텝 추가**
4. "B 페이지 세팅" 선택 → **+ 스텝 추가**
5. 두 스텝 모두 제출 방식 **수동**으로 설정 (기본값 확인)
6. **저장** → 그룹 목록에 "제휴사 세팅 테스트 (2스텝)"가 보이는지 확인
7. **통과 조건**: 그룹이 목록에 표시, 스텝 순서 A→B

### S4: 그룹 실행 (수동 제출 모드)
1. 그룹의 **실행** 버튼 클릭
2. **A 페이지가 새 탭으로 열리고** 폼이 자동으로 채워지는지 확인 (회사명/이메일/플랜 값 확인)
3. popup을 다시 열면 실행 상태 카드가 "제출 대기 중" + 배지에 `1/2` 표시 확인
4. A 페이지에서 직접 **submit 버튼 클릭** (제출 성공 시 form-a가 POST 처리된 결과 페이지로 이동하거나 아무 동작)
5. popup 다시 열기 → **다음 단계** 클릭
6. **B 페이지가 새 탭으로 열리고** 폼이 자동으로 채워지는지 확인 (name/phone/memo)
7. B 페이지 submit 클릭 → popup에서 **다음 단계** 클릭 (마지막 스텝)
8. **통과 조건**: 실행 상태 카드가 "완료!"로 표시, 배지가 사라짐(빈 상태), 그룹 목록 정상

### S5: 그룹 실행 중 중지
1. 그룹 **실행** 클릭 → A 페이지가 열리고 채워지는 것 확인
2. popup에서 **중지** 클릭
3. **통과 조건**: 실행 상태 카드가 "중지됨"으로 표시, 실행 중이던 탭이 닫힘, 이후 다른 그룹/프리셋 실행이 정상 동작 (중복 실행 차단이 풀렸는지 확인)

### S6: 자동 제출 모드 (선택, 가능한 경우)
1. 그룹 편집에서 스텝 A의 제출 방식을 **자동 제출**으로 변경, 제출 버튼 셀렉터 `button[type="submit"]` 입력 → 저장
2. 그룹 **실행** 클릭
3. **통과 조건**: A 페이지가 열리고 → 값 채워짐 → 약 1.2초 후 **제출 버튼이 자동으로 클릭됨** → 탭이 닫히고 B 페이지로 자동 진행

### S7: 회귀 (기존 프리셋 단일 기능)
1. 프리셋 탭에서 "A 페이지 세팅" **적용** 클릭
2. **통과 조건**: 기존 단일 프리셋 적용이 그대로 동작 (그룹 추가로 인한 회귀 없음)

## 4. 실패 처리 규칙

- 어떤 시나리오든 실패하면 **즉시 중단**하고 다음 정보를 보고합니다:
  1. 실패한 시나리오 번호와 단계
  2. 실제 관찰 결과 (스크린샷 첨부 가능)
  3. 기대했던 결과
  4. 콘솔 오류 (페이지 console, service worker console)
- 실패 원인이 명확하면 로그/스크린샷으로 증거를 남깁니다.

## 5. 최종 보고 형식 (반드시 이 형식)

```
# E2E 테스트 결과

- 확장 로드: ✅ / ❌
- S1 프리셋 A (캡처→적용): ✅ / ❌
- S2 프리셋 B (캡처→적용): ✅ / ❌
- S3 그룹 생성: ✅ / ❌
- S4 그룹 실행 (수동): ✅ / ❌
- S5 실행 중 중지: ✅ / ❌
- S6 자동 제출 (선택): ✅ / ❌ / ⏭️ 건너뜀
- S7 회귀 (단일 프리셋): ✅ / ❌

## 실패 상세 (해당 시)
- [시나리오] 단계: [증상] → [기대] / [콘솔 오류]

## 발견된 버그 (해당 시)
- [우선순위] [설명] [재현 방법]
```

---

## 기대 산출물 요약

```
빌드 확인 → 테스트 페이지 준비(form-a/b) → Playwright로 확장 로드
→ S1~S2 프리셋 캡처/적용 → S3 그룹 생성 → S4 그룹 실행(수동)
→ S5 중지 → S6 자동 제출(선택) → S7 회귀
→ 체크리스트 형식 최종 보고
```

실제 사용자가 하는 행동을 그대로 재현하며, 각 단계의 통과 조건을 명확히 검증하는 보고서가 산출물입니다.