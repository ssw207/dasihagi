# 메타 프롬프트: 녹화 방식 개선 — "녹화 → 평소대로 세팅 → 종료 → 재생"

## 역할

너는 Chrome 확장 프로그램(폼 프리셋, MV3)의 프리셋 등록 방식을 개선하는 시니어 프론트엔드 엔지니어다. 아래 요구사항을 **먼저 실제 코드베이스를 탐색한 뒤**, 최소 변경 원칙으로 구현하고 검증까지 완료해야 한다.

## 배경 / 현재 동작

- 현재 프리셋 등록은 **캡처 모드** 방식이다: 캡처 시작 → 페이지의 입력란을 하나씩 클릭 → 부동 패널에 **표시 이름** 입력 → Enter → **값** 입력 → Enter → 저장.
- **문제**: 필드마다 "표시 이름"을 직접 지정해야 하는 불편함이 있다. 값을 한 번만 입력하면 되는데, 이름과 값을 두 번 입력하고, 필드마다 클릭해야 한다.
- 기존 적용 모드는 저장된 필드를 **한꺼번에** 폼에 채운다(병렬 적용, 타이밍 없음).

## 목표 동작 (변경 후)

사용자는 **녹화 → 평소대로 세팅 → 종료**만 하면 되고, **재생** 버튼을 누르면 사용자가 폼을 채운 행동이 **순서와 타이밍 그대로 재생**된다. 필드마다 이름을 지을 필요가 없다.

1. **녹화 시작**: 프리셋에서 "녹화" 버튼 클릭 → 페이지가 녹화 모드 진입 (작은 부동 표시: `녹화 중 · N개` + **종료** 버튼).
2. **평소대로 세팅**: 사용자가 폼을 그냥 평소처럼 입력한다 (텍스트 타이핑, select 선택, checkbox/radio 체크). 확장은 각 입력을 **자동으로 기록**한다 — 이름 입력 없음, 값 재입력 없음, 필드 클릭 없음.
3. **종료**: 부동 표시의 **종료** 버튼 클릭 → 기록된 행동이 프리셋의 필드로 **자동 저장**된다.
4. **재생**: 프리셋의 "재생" 버튼 클릭 → 기록된 순서대로, 기록된 시간 간격으로 값이 채워진다.

## 핵심 설계 방향 (데이터 모델)

- **새 데이터 구조를 만들지 않는다.** 기존 프리셋의 `fields` 배열을 그대로 사용한다.
- 녹화된 각 행동 = 기존 필드(`id/label/selector/value/type`) + **`delay`**(직전 행동 이후 경과 ms) 한 가지 속성만 추가한다.
  - `label`은 기록 시점에 기존 `detectFieldLabel()`로 **자동 생성**한다 (placeholder → aria-label → name → label[for] → 부모 label → "필드").
  - `delay`는 재생 타이밍에만 사용되며, 기존 필드(캡처로 만든 것)에는 없어도 된다 → **하위 호환**.
- **재생 = 기존 적용 로직 확장**: 필드 중 하나라도 `delay`를 가지면 **순차 적용**(필드마다 `delay`만큼 대기 후 적용, 요소 지연 렌더링 대응으로 최대 5초 재시도), 없으면 기존 병렬 적용 그대로.
- 그룹 실행/자동 적용/민감값 암호화/내보내기·가져오기 등 기존 기능은 데이터 모델이 그대로이므로 **추가 수정 없이 동작**해야 한다.

## 구현 요구사항

### 1. 코드베이스 탐색 (구현 전 필수)
- content.js의 캡처 모드(상태 관리, 리스너, 패널), `detectFieldLabel()`, `generateSelector()`, `applyField()`, `applyPreset()` 위치 파악.
- background.js의 캡처 탭 상태(Set), `CAPTURE_SAVE_FIELD` 저장 로직, 민감 필드 정리 로직 파악.
- popup.js의 프리셋 카드 버튼(`적용/캡처/편집/삭제`)과 `handlePresetAction()`, 편집 뷰의 캡처 버튼 파악.

### 2. content.js — 녹화 모드
- `RECORD_START` 메시지 → 녹화 모드 시작: 부동 표시(`녹화 중 · N개` + 종료 버튼) 표시, `input`/`change` 리스너(캡처 단계) 등록.
- **기록 대상** (폼 필드만):
  - text/textarea: `input` 이벤트 → **400ms 디바운스** 후 최종 값 기록 (중간 타이핑 값 제외).
  - select: `change` 이벤트 → 선택된 값 기록.
  - checkbox/radio: `change` 이벤트 → `'true'`/`'false'` 기록.
- **기록 규칙**:
  - 각 행동: `{ id, label(자동), selector(generateSelector), value, type, delay }`.
  - `delay` = 직전 기록 시점부터의 경과 ms, **0~5000ms로 캡** (너무 긴 휴식은 재생 시 기다리지 않게).
  - **같은 필드를 연속 수정**하면 새 행동을 추가하지 않고 마지막 행동의 값을 갱신한다 (깨끗한 기록 유지).
  - 한글 IME 조합 중 `input`은 디바운스가 흡수하므로 별도 처리 불필요하지만, `isComposing` 중 Enter 등 오동작이 없어야 한다.
- `RECORD_STOP`/종료 버튼 클릭 → 리스너 해제, 부동 표시 제거, 기록된 행동을 `RECORD_SAVE` 메시지로 background에 전송. 행동이 0개면 저장하지 않는다.
- 녹화 모드는 페이지의 평소 폼 사용을 **방해하면 안 된다** (하이라이트/클릭 가로채기 금지 — 부동 표시만 표시).

### 3. content.js — 순차 재생
- `applyPreset()` 확장: 필드 중 하나라도 `delay`를 가지면 **순차 재생** 함수 사용.
  - 필드마다: `delay`(기본 150ms, 0~5000ms 캡) 대기 → 셀렉터로 요소 탐색(없으면 **최대 5초 폴링** — SPA 지연 렌더링 대응) → 기존 `applyField()`로 값 세팅 → 다음 필드.
  - 결과는 기존과 동일한 `{ applied, failures }` 형태.
- `delay`가 없는 기존 프리셋은 **기존 병렬 적용 로직 그대로** 유지.

### 4. background.js — 메시지 핸들러
- `RECORD_START`: 캡처 모드와 동일 패턴(탭 Set 관리 + content에 전파). 실패 시 "페이지에서 녹화를 시작할 수 없습니다..." 오류.
- `RECORD_STOP`: 탭 Set에서 제거 + content에 전파.
- `RECORD_SAVE`: `{ presetId, events }` → 프리셋의 `fields`를 이벤트 배열로 **교체** 저장.
  - 기존 민감 필드의 암호문(`sec:...`)은 전부 정리(`deleteSecret`) — 녹화가 전체를 교체하므로.
  - 이벤트에 `delay` 포함 저장, `updatedAt` 갱신.

### 5. popup — UI
- 프리셋 카드 버튼: `[적용] [캡처]` → **`[재생] [녹화]`** 로 교체 (편집/삭제 유지).
  - **재생** = 기존 `APPLY_PRESET` 메시지 재사용 (타이밍이 있으면 순차 재생됨). 토스트: "재생 완료: N개, 실패: M개".
  - **녹화** = `RECORD_START`. 토스트: "녹화 시작! 페이지에서 폼을 평소대로 입력한 뒤 [종료] 버튼을 누르세요." + popup 닫기.
- 편집 뷰: **"녹화 시작"** 버튼을 추가 (기존 "캡처 모드 시작" 유지, 둘 다 프리셋 저장 후 활성화).
- 기존 캡처 모드(이름 지정 방식)는 **유지**한다 — 세밀한 필드 제어가 필요한 사용자용.

### 6. 문서 갱신
- `README.md`: 5단계에 "녹화로 값 저장하기"를 주 방법으로, 캡처 모드를 보조 방법으로 설명. 6단계 "적용" → "재생"으로 표현 갱신.
- `REQUIREMENTS.md`: 새 요구사항(R9 녹화/재생) 추가, §4 데이터 모델에 `delay` 속성, §5 메시지에 `RECORD_START/STOP/SAVE`, §6 검증 체크리스트, §7 결정(D15), §9 변경 이력 갱신.
- `.sisyphus/notepads/form-preset-extension/decisions.md`에 D15 기록.

## 금지 사항 (MUST NOT)

- 기존 프리셋 데이터 구조(필드 스키마) 변경 금지 — `delay` 추가만 허용.
- 캡처 모드(이름 지정 방식) 제거·동작 변경 금지.
- 그룹 실행/자동 적용/민감값 암호화/내보내기·가져오기 로직 변경 금지 (동작은 그대로, 데이터 모델 공유로 자동 호환).
- 녹화 모드에서 페이지 하이라이트/클릭 가로채기 등 평소 사용 방해 금지.
- 타입 오류 억제(`as any`, `@ts-ignore`), 빈 catch로 통과 금지.
- 다른 프리셋 기능(목록, 삭제, 편집, 복사) 동작 변경 금지.

## 검증 (구현 후 반드시 수행)

- [ ] 녹화 시작 → 폼을 평소대로 입력(텍스트/select/checkbox) → 종료 시 필드가 자동 저장되는가? (label 자동 생성, 이름 입력 없음)
- [ ] 같은 필드를 수정하면 마지막 값으로 갱신되어 중복 행동이 없는가?
- [ ] 재생 버튼 → 기록된 순서와 타이밍(delay)대로 값이 채워지는가?
- [ ] `delay` 없는 기존 프리셋은 기존처럼 한 번에 적용되는가? (하위 호환)
- [ ] 그룹 실행/자동 적용이 녹화된 프리셋에서도 동작하는가?
- [ ] 민감 필드 체크 → 저장 → 조회 시 평문 미노출이 유지되는가? (기존 회귀 없음)
- [ ] 한글 IME 조합 중 오동작(조합 중간 값 기록)이 없는가?
- [ ] `npm run check` exit 0, `npm run build` exit 0, `npm run test:encrypt` 9/9 PASS.
- [ ] 변경 파일에서 LSP 진단 오류가 없는가?

## 참고 사항

- 변경 범위를 최소화하고 기존 코드 패턴(네이티브 JS, IIFE, 한국어 UI, escapeHtml)을 따라라.
- 작업 전에 관련 파일을 반드시 읽고, 의문점은 구현 전에 질문하라.