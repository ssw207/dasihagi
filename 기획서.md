# 다시하기 — 제품 기획서

> **문서 성격**: 구현된 제품을 기준으로 **역생성**한 기획서다. 앞으로 만들 기능의 약속이 아니라, **지금 제품이 무엇을 위해 존재하는지**를 한곳에 모은다.  
> **기준 시점**: 2026-09-05 · 버전 `0.1.0` · Chrome Extension Manifest V3  
> **기술 명세의 단일 기준**: [`REQUIREMENTS.md`](REQUIREMENTS.md) (R1~R19,R20~R24, D1~D48)  
> **사용자 매뉴얼**: [`README.md`](README.md)  
> **실사용자 시나리오**: [`유즈케이스-실사용자.md`](유즈케이스-실사용자.md)  
> **사내 테스트 한정 기안**: [`docs/기안-사내테스트-다시하기.md`](docs/기안-사내테스트-다시하기.md) **승인** (2026-08-30, D38). 기획팅 [`docs/기획팅-2026-08-30-사내테스트-기안.md`](docs/기획팅-2026-08-30-사내테스트-기안.md)

---

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 제품 표시 이름 | 다시하기 |
| 저장소 / 패키지 | dasihagi (`ssw207/dasihagi`) |
| 이전 이름 | form-preset-extension (가져오기 JSON의 옛 `appId`는 계속 허용) |
| 형태 | Chrome 확장 프로그램. 로그인한 사용자 Chrome에서 동작 |
| UI 언어 | 한국어만 |
| 배포 | 개발자 모드로 `dist/` 로드. 웹스토어 배포는 이 문서 범위 밖. 사내 사용은 테스트 환경 한정 기안 참고 |
| 작성 방식 | 코드·README·요구사항·결정(D1~D39)·유즈케이스를 역추적 |

관련 문서를 나누는 기준:

| 문서 | 읽는 사람 | 역할 |
|------|-----------|------|
| 이 기획서 | PO, 사용자, 처음 보는 기여자 | 문제·범위·하지 않는 일 |
| docs/기안-사내테스트-다시하기.md | 승인권자, 보안/IT, 파일럿 부서 | 사내 테스트 한정 개발·파일럿 (2026-08-30 승인) |
| REQUIREMENTS.md | 구현 에이전트 | 수용 기준, 데이터, 메시지 |
| decisions.md | 구현 에이전트 | 왜 그렇게 만들었는지 |
| README.md | 설치하고 쓰는 사람 | 설치·사용·문제 해결 |
| 유즈케이스-실사용자.md | PO·디자인·프론트 | 누가 쓰는지 교차 검증 |

---

## 2. 배경과 문제

같은 웹 화면에 **거의 같은 값**을 여러 번 넣는다.

사내 어드민 제휴 설정, 테스트 계정 전환, 기안 양식, 로컬 스테이징 로그인처럼 필드 집합은 고정이고 값의 80%는 반복된다. 엑셀·메모장에서 복사하다 한 칸이 틀리고, 체크박스를 빼먹는다.

기존 도구가 잘 안 맞는 지점:

- **비밀번호 관리자**: 로그인 칸에는 강하다. 제휴 설정 30칸, 약관 체크, 셀렉트에는 약하다.
- **매크로 / RPA / Playwright 스크립트**: 개발자 도구다. 이미 로그인해 둔 Chrome 프로필, 폐쇄망 어드민, “버튼 하나 누르고 확인”과는 설치·배포가 다르다.
- **엑셀 일괄 업로드**: 사이트가 받아 주지 않으면 쓸 수 없다.

필요한 것은 **지금 보고 있는 페이지에서 한 일을 저장하고, 다음에 같은 순서로 다시 하는 일**이다. 제출·결재·결제는 사람이 한다.

---

## 3. 제품 정의

### 3.1 한 줄

사이트에서 한 번 한 **입력·클릭·이동**을 녹화해 두고, **재생**으로 다시 실행한다.

### 3.2 포지셔닝

| | 다시하기 | 아닌 것 |
|--|----------|---------|
| 실행 장소 | 사용자가 이미 쓰는 Chrome | 별도 데스크톱 RPA, Node 스크립트 |
| 대상 | 반복 폼·고정 DOM 어드민·로컬 | 좌석 오픈런, 대기열, 캡차 |
| 단위 | 프리셋(한 흐름 또는 한 폼), 그룹(폼을 순서대로) | 사이트 전용 스크래퍼 |
| 제출 | 기본은 채움. 자동 제출은 그룹에서만 사용자가 고름 | 결제·상신을 대신 누르기 |
| 데이터 | 이 Chrome 프로필 안 암호화 저장 | 서버 동기화, 계정 로그인 |

이름 **다시하기**는 폼 프리셋만 가리키지 않는다. 클릭·페이지 이동·iframe·그룹까지 포함한 뒤, “한 일을 다시 한다”가 제품의 중심이 되었다 (D33). “매크로”, “자동화”, “봇”이라는 말은 쓰지 않는다.

### 3.3 제품 원칙

1. **사이트 하드코딩 없음.** 특정 포털 셀렉터를 코드에 넣지 않는다.
2. **개발자가 아닌 한국어 사용자.** 화면 글은 한국어. 셀렉터는 기본 접힌다.
3. **제출은 사람이 안전하게.** 재생은 칸을 채우고 녹화한 클릭만 재현한다.
4. **값은 이 프로필 안에.** 서버로 올리지 않는다. 다른 PC는 내보내기 파일로만.
5. **도구를 제품으로 바꾸지 않는다.** Playwright는 E2E 검증용이다 (D21).

---

## 4. 목표와 비목표

### 4.1 목표 (지금 제품이 하는 일)

- 현재 사이트에서 **이 사이트 녹화** 한 번으로 프리셋을 만들고 바로 녹화한다.
- 녹화 종료 후 **재생**으로 같은 입력·클릭·이동을 다시 실행한다.
- 페이지마다 폼만 있으면 **그룹**으로 프리셋을 순서대로 연다.
- 검색 → 클릭 → 이동처럼 한 탭 흐름이면 **여정**으로 한 프리셋에 담는다.
- 휴대폰·이메일처럼 보이는 값은 목록에서 가리고, 저장소에는 프리셋 전체를 암호화한다.
- 사내(봇 제한 없음)와 외부 사이트를 **재생 속도**로 나눈다. 도메인을 코드에 넣지 않는다.

### 4.2 비목표 (하지 않는 일)

README와 같다. 기획에서 약속하지 않는다.

- 좌석 오픈런, 대기열, 캡차, 보안 키패드
- 파일·이미지 첨부 (`input[type=file]`)
- `chrome://` 등 크롬 내부 페이지
- sandbox로 막힌 iframe, Shadow DOM 전용 위젯, 데스크톱 전용 프로그램
- 비밀번호 관리자를 대체하는 자동 로그인 제품
- 엑셀을 폼에 대량 업로드
- 계정 서버, 팀 공유, 클라우드 동기화
- iOS 크롬 확장 (데스크톱 Chrome 전제)

결제·상신·전송 버튼은 녹화가 찍혀 있어도 **직접 누르는 편이 안전하다**고 안내한다.

---

## 5. 사용자

전제: **개발자가 아닌 한국어 사용자.** 같은 Chrome 프로필에서 반복한다.

### 5.1 핵심 페르소나 (P0)

| ID | 누구 | 하는 일 | 제품에서 쓰는 것 |
|----|------|---------|------------------|
| U1 | 제휴 운영 | 제휴사만 다르고 필드 30칸이 같다. 주 수회~수십 회 | 그룹 + 프리셋 여러 개 + 재생 속도 **빠름** |
| U2 | QA / 기획 | 구매자A / 판매자B / 관리자 값을 바꿔 가며 같은 폼 | 프리셋 전환, 민감 마스킹, 클릭 복사, 현재 사이트만 |
| U8 | 개발·스테이징 | 매일 localhost 로그인·시드 데이터 | 포트 포함 패턴, 암호화, (선택) 자동 적용 |

이 세 가지는 지금 기능과 겹친다. 폐쇄망·고정 셀렉터에서 가장 강하다.

### 5.2 잘 맞는 공통 패턴

1. 필드 집합이 안정하다 (어드민, 사내, 로컬).
2. 값의 80%가 반복되고 20%만 손으로 고친다.
3. 제출·결재·결제·좌석은 사람이 한다.
4. 같은 크롬 프로필에서 반복한다.

### 5.3 부분적으로만 맞는 경우

웹 그룹웨어 기안(U3), 온보딩 서류(U5), CRM 템플릿(U6), 웹 전표(U7), 상품 공통정보(U12)는 **텍스트·셀렉트·체크**면 된다. 레거시 ActiveX, 파일 첨부, 이미지 업로드, 자체 브라우저는 범위 밖이다.

예매(U4)·공공 민원(U14)은 **인적사항 채움만** 말하고, 좌석·인증·보안키패드는 비추천으로 못 박는다.

---

## 6. 가치와 성공 조건

### 6.1 사용자에게 주는 것

| 가치 | 측정에 가까운 성공 |
|------|-------------------|
| 반복 입력 시간 감소 | 제휴 한 건이 수 분 → 수십 초 (재생 후 이름·사업자만 수정) |
| 누락 감소 | 약관·부가세 유형 등 고정 체크가 빠지지 않음 |
| 페르소나 전환 | 테스트 계정 전환이 클릭 한 번 |
| 신뢰 | 목록에서 민감값이 `••••••`, 저장소 평문에 필드 값 없음 |
| 첫 성공 | 연습 페이지에서 녹화 → 칸 지우기 → 재생으로 값이 돌아옴 (README 5분) |

### 6.2 제품 쪽 성공 (현재 검증)

기능이 “있다”는 말은 아래가 초록일 때와 같다.

- `npm run check` / `build`
- `test:encrypt` 13/13 · `test:pii` 30/30 · `test:replay` 161/161 · `test:qa` 22/22
- `test:e2e` 71/71 — 실제 Chromium에 `dist` 로드, 시나리오 A~L (로컬 페이지. 실사이트·로그인은 검증하지 않음)

---

## 7. 기능 범위

사용자 언어. 괄호는 요구사항 ID.

### 7.1 In — 지금 됨

**프리셋**

- 사이트별 이름·패턴·필드 목록을 만들고 고치고 지운다. 여러 개 선택 삭제 (R1, R14).
- 사이트 패턴: 호스트, `*.example.com`, 경로, `host:port`. 한 줄에 하나, 최대 20개. 녹화 중 방문한 http(s) 사이트는 종료 시 자동 추가 (R12).
- 페이지 진입 시 자동 적용. **여정 프리셋은 자동 적용하지 않는다** (R1-4, R11-7).

**녹화와 재생** (주 동선, R9·R11·R15)

- **이 사이트 녹화**: 현재 호스트로 항목을 만들고 바로 녹화.
- 평소처럼 입력·클릭. 화면 위 빨간 **종료** 또는 팝업 **종료**.
- 기록: 텍스트(디바운스), 셀렉트/체크/라디오, 클릭, Enter, URL 이동. 같은 페이지 입력 레이어(열기 → 입력 → 확인 닫힘)도 시간 순으로 남긴다. 클릭을 가로채지 않는다.
- 페이지가 바뀌어도 같은 탭에서 녹화가 이어진다. A가 연 팝업/새 탭도 같은 세션 (R16).
- 사내 어드민이 iframe 안이어도 그 프레임에서 녹화·재생 (R18). 껍데기 이후 늦게 뜨는 iframe도 활성 녹화에 합류한다. 작은 광고 iframe에는 칩을 올리지 않는다. sandbox iframe은 불가.
- **재생**: 여정이면 같은 탭에서 순서대로. 폼만이면 값 채움. 클릭 셀렉터가 깨지면 보이는 글자로 한 번 더 찾는다.
- **손댈 칸**: 비워 둔 채움 칸은 재생이 덮지 않고 노랗게 남긴다. 목록에는 `나중에 입력`. 편집에서 켜고 끌 수 있다. 칸을 못 찾은 경우만 누락 (R19).
- **재생 금지**: `상신·결재·결제·구매하기·전송·송금·이체·Pay` 같은 되돌리기 어려운 클릭은 재생이 누르지 않고 `직접 누르기`로 멈춘다. 편집에서 켜고 끌 수 있다. 그룹 auto 제출도 금지면 대기 (R20).
- **파일 첨부**: `input[type=file]` 값은 채우지 않고 `직접 첨부`로 안내 (R21).
- **다시 찍기**: 필드 하나 셀렉터가 깨지면 그 칸만 다시 지정한다. 재녹화 없음, 캡처와 같은 동작 (R23).
- **프리셋별 재생 속도**: 전역은 유지하되 프리셋에 `빠름/보통/느림`이 있으면 그쪽이 이긴다 (R22).
- **재생 속도** 빠름 / 보통 / 느림 — 페이지 이동 뒤 쉬는 시간 (R17, 전역. 프리셋이 덮음). 사내는 빠름.

**캡처** (보조 동선, R2)

- 녹화로 빠진 칸만. 파란 점선 → 클릭 → 표시 이름 Enter → 값 Enter.

**그룹** (R4)

- 이미 만든 프리셋을 스텝으로 연결. 실행 시 스텝마다 **새 탭**.
- 스텝마다 수동(값만) / 자동 제출(셀렉터 클릭). 배지 `N/M`, 중지.

**값 UX** (R7, R10, R13)

- 카드에서 값 행·복사. 민감은 `••••••`. 손댈 칸은 `나중에 입력`. 필드 이름 편집. 5개 초과 더보기.

**백업** (R5)

- 내보내기 JSON에는 **실제 값** 또는 **값 없는 스크럽**(값만 비움, 그룹 포함, 손댈 칸·금지·속도 표시는 유지). 가져오기는 기존 유지 + 새 항목. 상한: 프리셋 500, 그룹 100, 파일 5MB. (R5, R24)

**저장 보호** (R6)

- 프리셋 전체 `sec:preset:<id>` AES-256-GCM. 목록 매칭용 인덱스만 평문.

### 7.2 Out — 명시적으로 안 함

| 항목 | 이유 |
|------|------|
| 전용 options 페이지 | popup과 중복 (D10, D14) |
| Playwright를 제품 엔진으로 | 스토어 배포·로그인된 Chrome과 안 맞음 (D21) |
| 필수 칸 비움 경고 | 손댈 칸과 다름. 사이트 필수 표시는 없음 |
| 팀/클라우드 공유 | 로컬 프로필만 (스크럽은 값 없이 구조만) |
| 파일 첨부 재생 | 확장이 채울 수 없음 (직접 첨부 안내만) |

### 7.3 여정과 그룹을 나누는 법

사용자가 가장 헷갈리는 결정. 제품이 두 모델을 유지하는 이유:

| | 여정 | 그룹 |
|--|------|------|
| 언제 | 검색 → 클릭 → 이동처럼 **한 흐름** | 페이지마다 **폼만** 순서대로 (제휴사 A→B→C) |
| 저장 | 프리셋 하나 | 이미 만든 프리셋을 스텝으로 |
| 재생 | 같은 탭 | 스텝마다 새 탭 |
| 제출 | 녹화한 클릭만 | 스텝을 수동 또는 자동 제출 |
| 자동 적용 | 없음 | 각 프리셋 설정에 따름 (여정 스텝은 해당 없음) |

한 화면에서 검색·클릭이 필요하면 여정. 서로 다른 URL의 설정 폼이면 그룹.

---

## 8. 정보 구조와 화면

확장 popup. 모든 관리가 여기서 끝난다 (options 페이지 없음).

```
다시하기
├── 헤더: 제품명 + 현재 사이트 칩
├── 탭: 프리셋 | 그룹
├── (조건부) 녹화 중 배너 · N개 [종료]
├── (조건부) 그룹 실행 상태
├── 프리셋
│   ├── 이 사이트 녹화 | + 새 프리셋 | 더보기(내보내기/가져오기)
│   ├── 현재 사이트만 (기본 켜짐)
│   ├── 재생 속도: 빠름 / 보통 / 느림
│   ├── 선택 삭제 툴바
│   └── 카드: 이름, 여정 배지, 값 미리보기, [재생] [⋯ 녹화/편집/삭제]
├── 그룹
│   ├── + 새 그룹
│   └── 카드: 스텝 수, [실행]
├── 프리셋 편집
│   ├── 이름, 사이트 패턴, 자동 적용
│   ├── 필드 목록 (이름 수정, 복사, 민감 표시)
│   └── 녹화 시작 | 캡처 모드
└── 그룹 편집
    └── 스텝 추가, 제출 방식, 제출 셀렉터
```

페이지 위(content):

- 녹화 중: 검정 칩 `녹화 중 · N개` + 빨간 종료. 큰 iframe 또는 top만.
- 캡처 중: 입력 파란 점선, 화면 가운데 부동 패널.
- 적용 직후: 해당 칸 초록 테두리 깜빡임.

빈 상태 카피의 역할: 아직 없으면 녹화로 시작하고, 칸만 저장하려면 새 프리셋 + 캡처, 폼만 여러 사이트면 그룹, 클릭·이동이면 여정.

---

## 9. 핵심 사용자 흐름

### 9.1 첫 성공 (5분)

1. 연습 페이지를 연다 (`test/test-form.html` 또는 실제 사내 폼).
2. **이 사이트 녹화** → 평소처럼 입력 → **종료**.
3. 칸을 지운 뒤 **재생**. 값이 돌아오면 설치가 끝난 것이다.

### 9.2 일상: 같은 폼을 여러 번

1. 해당 사이트에서 팝업을 연다 (현재 사이트만 기본).
2. 카드 **재생**. 바뀌는 칸만 손으로 고친다.
3. 제출은 사이트의 버튼을 직접 누른다.

### 9.3 여정: 검색 후 이동

1. 시작 페이지에서 녹화.
2. 검색, 결과 클릭, 다음 페이지 입력. 팝업이 열려도 이어진다.
3. 종료하면 방문 사이트가 패턴에 붙는다.
4. 다음에 재생하면 시작 URL로 맞춘 뒤 같은 탭에서 진행한다.

광고·로그인·검색 순위가 자주 바뀌면 그 단계만 다시 녹화한다. 동적 사이트 실패는 제품 한계다.

### 9.4 그룹: 제휴사 A→B→C

1. 페이지마다 프리셋을 녹화한다.
2. 그룹에 순서대로 넣는다. 저장이 위험하면 스텝을 **수동**.
3. **실행**. 아이콘에 `2/3`. **중지** 가능.

### 9.5 값만 복사

목록 카드에서 복사. 민감은 가려져 있어도 복사는 실값. 엑셀 왕복 실수를 줄이는 U2·U16 동선.

---

## 10. 데이터·보안·프라이버시

사용자에게 세 문장으로 말한다.

1. **값은 이 Chrome 프로필 안에서 암호화되어 저장된다.** 프리셋 통째 (`sec:preset:<id>`, AES-256-GCM). 자동 적용 매칭용 이름·사이트만 평문 인덱스다.
2. **휴대폰·이메일·비밀번호처럼 보이는 칸은 목록에서 가린다.** 감지는 마스킹용이다. 감지가 빠져도 저장소 평문에는 값이 없다.
3. **내보내기 파일에는 실값이 들어간다.** 파일을 안전하게 보관한다. 다른 PC는 가져오기로만 옮긴다.

보호 수준 (과장 금지): 로컬 열람, 백업 유출, 문자열 검색 방어. OS 키체인 수준은 확장만으로 불가. 키가 없거나 손상되면 이 프로필의 예전 항목을 풀 수 없다.

필드 값 상한 64KB, 프리셋당 필드 500, 사이트 패턴 20.

권한 (`manifest.json`): `storage`, `tabs`, `scripting`, `webNavigation`. content script는 `<all_urls>` + `all_frames`. 교차 origin iframe DOM을 부모가 읽지 않는다. 각 프레임의 스크립트가 자기 문서만 다룬다.

XSS: popup/content에 동적 값을 넣을 때 이스케이프.

---

## 11. 제약·한계·리스크

| 영역 | 강한 곳 | 깨지기 쉬운 곳 |
|------|---------|----------------|
| DOM | 폐쇄망 고정 폼, 일반 input/select/checkbox | 가상 리스트 좌석, 자주 바뀌는 검색 결과 |
| 프레임 | 같은 탭 iframe (all_frames) | sandbox iframe, 아주 작은 iframe(칩 없음) |
| 창 | opener가 있는 팝업/새 탭 이어 녹화 | 무관한 창, chrome:// 팝업 |
| 적용 | 네이티브 setter + input/change (React/Vue) | Shadow DOM, contenteditable 전용 에디터 |
| 매칭 | 호스트·경로·포트 | 사이트 개편 후 셀렉터 무효 |
| 플랫폼 | 데스크톱 Chrome | iOS, 자체 브라우저, 네이티브 앱 |

재생이 실패한 기본 대응: 그 단계만 다시 녹화하거나 캡처로 칸을 보완. 매크로 재시도·봇 우회는 넣지 않는다.

---

## 12. 비기능·배포

| 항목 | 결정 |
|------|------|
| 런타임 | Manifest V3, service worker, 네이티브 JS, 번들러 없음 (D1) |
| 산출물 | `npm run build` → `dist/` |
| 의존성 | 런타임 없음. 개발: Playwright(E2E), ffmpeg(README GIF) |
| 설치 | 저장소 clone → build → `chrome://extensions` 압축 해제 로드 |
| 업데이트 | `git pull` → build → 확장 새로고침 |
| UI | 한국어. popup 단일 관리 화면 |

---

## 13. 로드맵 (역생성)

구현을 시간순이 아니라 **제품 단계**로 재구성한다. 날짜는 모두 2026-08-16 전후 집중 구현.

| 단계 | 제품으로 보면 | 상태 |
|------|----------------|------|
| 1 | 사이트별 폼 프리셋, 캡처, 적용, 자동 적용 | 완료 (R1~R3) |
| 2 | 그룹으로 여러 페이지 폼 | 완료 (R4) |
| 3 | 백업, 값 복사, 민감 마스킹, 전체 암호화 | 완료 (R5~R7, R6, R10) |
| 4 | 녹화/재생이 주 동선. 캡처는 보완 | 완료 (R9, R15) |
| 5 | 여정(클릭·이동·팝업 이어짐), 다중 사이트, iframe, 재생 속도 | 완료 (R11, R12, R16~R18) |
| 6 | 선택 삭제, 필드 이름 수정, 로컬 E2E | 완료 (R13, R14, R8) |

**보류 (의도적으로 안 함)**

- options_page
- `프리셋-정보-관리` 별도 구현체 병합 (가치 기능은 흡수됨)

**후보 (약속 아님, 유즈케이스·UI 검토에서만 나온 것)**

- 프리셋별 재생 속도 (집/회사 전환)
- ~~빈 칸을 “일부러 비움”으로 보여 주기~~ → 손댈 칸/나중에 입력으로 해소 (R19, D43)
- 첨부/이미지는 못 채운다는 사전 안내
- 재생 ≠ 제출을 더 강하게 보여 주기

다음 기능을 넣을 때도 이 기획서의 비목표를 먼저 본다.

---

## 14. 용어

| 말 | 뜻 |
|----|----|
| 프리셋 | 한 사이트(들)에 대한 저장 단위. 폼만 있거나 여정일 수 있다 |
| 필드 | 저장된 한 행동. 값 채움이거나 click / keydown / navigate |
| 녹화 | 평소 사용을 관찰해 필드를 쌓는다 |
| 재생 | 저장한 순서대로 다시 실행한다. 그룹의 “실행”과 구분 |
| 캡처 | 칸을 직접 고르고 이름·값을 넣는 보조 등록 |
| 여정 | 클릭·이동이 들어 있는 프리셋. 같은 탭 재생 |
| 그룹 | 프리셋을 스텝으로 이은 목록. 새 탭 순차 실행 |
| 자동 적용 | 페이지 진입 시 폼 프리셋 채움. 여정 제외 |
| 민감 | 목록 마스킹 신호. 저장 암호화와 별개 |
| 사이트 패턴 | 이 프리셋이 허용하는 URL. 대표 하나 + N개 |

---

## 15. 부록 — 요구사항 매핑

기획 기능 → 수용 기준이 있는 곳.

| 기획 | REQUIREMENTS |
|------|----------------|
| 프리셋 CRUD·패턴·자동 적용 | R1 |
| 캡처 | R2 |
| 적용 (SPA 호환) | R3 |
| 그룹 | R4 |
| 내보내기/가져오기 | R5 |
| 스크럽(값 없이) | R24 |
| 전체 암호화 | R6 |
| 클릭 복사·마스킹 | R7 |
| 자동 검증 | R8 |
| 녹화/재생 | R9 |
| PII 감지(마스킹) | R10 |
| 여정 | R11 (모달 레이어 R11-10) |
| 다중 사이트 | R12 |
| 필드 이름 수정 | R13 |
| 여러 개 삭제 | R14 |
| 이 사이트 녹화 등 P0 UX | R15 |
| 팝업 이어 녹화 | R16 |
| 재생 속도(전역) | R17 |
| 프리셋별 속도 | R22 |
| iframe | R18 |
| 손댈 칸 | R19 |
| 위험 클릭 재생 금지 | R20 |
| 파일 첨부 멈춤 | R21 |
| 깨진 칸만 다시 찍기 | R23 |

아키텍처 결정 요약은 REQUIREMENTS §7, 상세는 `.sisyphus/notepads/form-preset-extension/decisions.md`.
