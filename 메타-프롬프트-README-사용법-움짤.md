# 메타 프롬프트: README 사용법 움짤

## 역할

폼 프리셋 README에 **실제 확장 화면**으로 찍은 GIF를 넣는다. AI가 그린 가짜 UI는 쓰지 않는다.

## 사용자 요구 원문

> README에 사용하는방법 움짤로 만들어서 넣어 줄수있어?

## 목표

1. Playwright로 `dist` 확장을 로드해 테스트 페이지에서 사용 흐름을 촬영한다.
2. ffmpeg(없으면 `ffmpeg-static` 일회 사용)로 GIF로 바꾼다.
3. README 사용 단계 옆에 넣는다. 재촬영 스크립트를 남긴다.

촬영 대상 (짧게, 읽기 쉬운 속도):
- 프리셋 만들기 (popup)
- 녹화 → 입력 → 종료 (페이지 + 칩)
- 재생으로 값이 채워짐
- (가능하면) 여정: 검색 → 더보기 → 상품

## MUST NOT

- 네이버 실접속, 개인정보 실값
- 가짜 목업 이미지로 대체
- 기존 E2E A~E 시나리오 삭제
- ffmpeg를 제품 런타임 의존성으로 넣기 (dev/일회만)

## 검증

- [x] GIF 파일이 저장소에 있고 README에서 상대경로로 보이는가
- [x] 실제 확장 UI(녹화 칩, 재생)가 프레임에 있는가
- [x] `npm run check` 유지

재생성: `npm run gif` → `docs/gif/*.gif`
