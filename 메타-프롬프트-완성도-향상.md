# 메타프롬프트 — 완성도 향상 (스스로 수정)

## 목표

사용자 요구 원문: **「정말 판단하기 어려운것 제외하고 물어보지 말고 스스로 진행하고 수정하며 완성도를 높일것」**

미커밋 워킹트리(R11~R14 포함)를 기준으로, 질문 없이 명확한 결함·불일치·미완을 고치고 검증한다. 판단이 갈리는 신규 기능(대규모 UI 개편, 제품 방향)은 넣지 않는다.

## 변경 범위

- `background.js`, `content.js`, `popup/*`, `secure-store.js` — 버그/방어/일관성
- `test/*` — 회귀 보강 (기존 케이스 삭제 금지)
- `REQUIREMENTS.md`, `.sisyphus/notepads/form-preset-extension/decisions.md`, `README.md`, `.sisyphus/plans/*.md` — 코드와 문서 정합
- `dist/` — `npm run build` 후 동기화
- fill-form 분리 워크트리의 **명확히 더 나은 수정만** master 워킹트리에 흡수

## 구현 상세

1. master 워킹트리와 fill-form 워크트리 diff를 대조해 누락된 수정만 흡수.
2. QA 메타프롬프트에 적힌 확정 버그가 코드에 반영됐는지 확인하고 빠진 것만 고친다.
3. `npm run check` / `test:encrypt` / `test:pii` / `test:replay` 실행 후 실패를 고친다. E2E는 환경이 되면 실행.
4. 플랜 체크박스가 코드와 어긋나면 완료로 맞춘다.
5. REQUIREMENTS/decisions/README를 실제 동작에 맞게 맞춘다.

## MUST NOT

- 사용자에게 쉬운 선택지를 물어보지 않는다.
- 대규모 UI 리디자인, options_page 추가, Playwright를 제품 런타임으로 교체하지 않는다.
- 기존 테스트 삭제·빈 catch로 통과시키지 않는다.
- 커밋은 사용자가 요청하지 않았으므로 하지 않는다.
- 사이트 하드코딩 금지.

## 검증 방법

- `npm run check` exit 0
- `npm run test:encrypt` 전부 PASS
- `npm run test:pii` 전부 PASS
- `npm run test:replay` 전부 PASS
- 가능하면 `npm run test:e2e`
- `npm run build` 후 dist가 소스와 일치
