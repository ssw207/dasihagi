{
  "name": "form-preset-extension",
  "private": true,
  "version": "0.1.0",
  "description": "사이트별 폼 자동 세팅 프리셋 Chrome Extension",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check pii-detect.js && node --check secure-store.js && node --check popup/popup.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest OK')\"",
    "build": "node scripts/build.js",
    "test:encrypt": "node --experimental-default-type=module test/encryption-verify.mjs",
    "test:pii": "node --experimental-default-type=module test/pii-detect-verify.mjs",
    "test:replay": "node --experimental-default-type=module test/record-replay-verify.mjs",
    "test:e2e": "npm run build && node --experimental-default-type=module test/browser-e2e-verify.mjs",
    "gif": "npm run build && node --experimental-default-type=module scripts/record-readme-gifs.mjs"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.62.1"
  }
}
